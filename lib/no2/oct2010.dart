import 'package:flutter/material.dart';

class oct2010 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('2010'),
        ),
        body: Image.asset('assets/oct2010.jpg',)
    );
  }
}