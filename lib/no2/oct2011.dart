import 'package:flutter/material.dart';

class oct2011 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('2011'),
        ),
        body: Image.asset('assets/oct2011.jpg',)
    );
  }
}