import 'package:flutter/material.dart';

class oct2016 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('2016'),
        ),
        body: Image.asset('assets/oct2016.jpg',)
    );
  }
}